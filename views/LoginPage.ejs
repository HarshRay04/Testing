<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login Page</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .cover-container {
      background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.4)),
        url("/assets/p2.png") no-repeat center center fixed;
      background-size: cover !important;
      color: #fff;
      border-radius: 12px;
      padding: 2rem;
      width: 100%;
      height: 100vh;
      position: relative;
    }

    /* Rent Tracker header */
    .header {
      position: absolute;
      top: 20px;
      left: 30px;
      font-size: 22px;
      font-weight: bold;
      color: #62c5e9;
      text-shadow: 0px 2px 5px rgba(0, 0, 0, 0.7);
      cursor: pointer;
    }

    .header:hover {
      color: #1486a3;
    }

    .card {
      width: 350px;
      padding: 30px;
      border-radius: 15px;
      background: rgba(0, 0, 0, 0.6);
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.6);
      text-align: center;
    }

    .card h2 {
      margin-bottom: 20px;
      color: #ffffff;
      text-shadow: 0px 2px 5px rgba(0, 0, 0, 0.7);
    }

    .card input {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      border: none;
      border-radius: 8px;
      background: rgba(255, 255, 255, 0.15);
      border: 1px solid rgba(255, 255, 255, 0.3);
      color: #ffffff;
      font-size: 14px;
      outline: none;
    }

    .card input::placeholder {
      color: #dddddd;
      opacity: 0.8;
    }

    .card .btn-personal {
      width: 100%;
      padding: 12px;
      margin-top: 15px;
      border: none;
      border-radius: 8px;
      background: #62c5e9;
      color: #000;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }

    .card .btn-personal:hover {
      background: #1486a3;
      color: #fff;
    }

    .btn-2 {
      width: 100%;
      padding: 12px;
      margin-top: 15px;
      border: none;
      border-radius: 8px;
      background: #62c5e9;
      color: #000;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }

    .btn-2:hover {
      background: #1486a3;
      color: #fff;
    }

    .card a {
      display: block;
      margin-top: 15px;
      color: #ffffff;
      text-decoration: none;
      font-size: 14px;
      opacity: 0.9;
    }

    .card a:hover {
      text-decoration: underline;
    }
  </style>
</head>

<body class="cover-container">

  <!-- Rent Tracker header -->
  <div class="header">
    <h3 class="float-md-start mb-0 fw-bold">
      <a href=" /" class="float-md-start mb-0 fw-bold text-decoration-none nav-link fw-bold">Rent Due Tracker</a>
    </h3>
  </div>

  <div class="card">
    <h2>Login</h2>
    <form>
      <input type="text" placeholder="Enter Username" required>
      <input type="password" placeholder="Enter Password" required>
      <button class="btn-personal" type="submit">Login</button>
    </form>
    <a href="#">Forgot Password?</a>
    <button class="btn-2" onclick="window.location.href='home.html'">Go Home</button>
  </div>

</body>

</html>